# Example environment variables
# Copy this file to .env.local and adjust values for your setup

# API Configuration
NEXT_PUBLIC_API_URL=https://your-server.com/api
NEXT_PUBLIC_API_DEV_IP=https://192.168.x.x/api

# Frontend Development Port
PORT=5164

# Environment
NODE_ENV=development
NEXT_PUBLIC_ENV=development

# Development specific flags
# Set to true to use NEXT_PUBLIC_API_DEV_IP instead of NEXT_PUBLIC_API_URL
NEXT_PUBLIC_USE_DEV_IP=false

# SSL (for self-signed certificates in development)
# Set to 0 to disable SSL verification for development with self-signed certificates
# NODE_TLS_REJECT_UNAUTHORIZED=0